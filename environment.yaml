channels:
  - conda-forge
dependencies:
  # dependencies
  - xtensor

  # optional dependencies
  - xsimd

  # build tools
  - cmake
  - setuptools_scm

  # Python API
  - python
  - pyxtensor

  # run tests
  - catch2
  - highfive
  - numpy
  - h5py

  # run examples
  - prrng
  - python-prrng

  # build docs
  - doxygen
  - mathjax
